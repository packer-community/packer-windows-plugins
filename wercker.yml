box: wercker/golang@1.2.1
build:
    steps:
        - setup-go-workspace
        - script:
            name: go get
            code: |
                go get -t ./...
        - script:
            name: go test
            code: |
                make test -v ./...
        - tcnksm/goveralls:
            token: $COVERALLS_TOKEN
